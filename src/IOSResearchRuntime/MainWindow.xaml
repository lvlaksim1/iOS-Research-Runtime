<Window x:Class="IOSResearchRuntime.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iOS Research Runtime"
        Width="1120"
        Height="720"
        MinWidth="900"
        MinHeight="560"
        WindowStartupLocation="CenterScreen"
        Background="#F4F5F7">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="iOS Research Runtime"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#111827"/>
            <TextBlock Margin="0,6,0,0"
                       Text="Windows-native runtime для загрузки и исследования iOS/Darwin"
                       FontSize="14"
                       Foreground="#6B7280"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,22,0,18"
                Padding="16"
                CornerRadius="10"
                Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Состояние" FontSize="12" Foreground="#6B7280"/>
                    <TextBlock x:Name="StatusText"
                               Margin="0,4,0,0"
                               Text="Проверка среды…"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#111827"/>
                    <TextBlock x:Name="DetailsText"
                               Margin="0,5,0,0"
                               TextWrapping="Wrap"
                               Foreground="#6B7280"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="PrepareButton"
                            MinWidth="160"
                            Margin="0,0,8,0"
                            Padding="14,9"
                            Content="Подготовить среду"
                            Click="PrepareButton_Click"/>
                    <Button x:Name="StartButton"
                            MinWidth="110"
                            Margin="0,0,8,0"
                            Padding="14,9"
                            Content="Запустить"
                            Click="StartButton_Click"/>
                    <Button x:Name="StopButton"
                            MinWidth="110"
                            Padding="14,9"
                            Content="Остановить"
                            Click="StopButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Padding="1"
                CornerRadius="10"
                Background="#D1D5DB">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Padding="14,10" Background="#FFFFFF" CornerRadius="9,9,0,0">
                    <TextBlock Text="Provisioning / boot log"
                               FontWeight="SemiBold"
                               Foreground="#374151"/>
                </Border>
                <TextBox x:Name="LogTextBox"
                         Grid.Row="1"
                         Padding="14"
                         BorderThickness="0"
                         Background="#111827"
                         Foreground="#E5E7EB"
                         FontFamily="Consolas"
                         FontSize="12"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="3"
                   Margin="0,14,0,0"
                   Text="0.1.0 • Milestone 1: Windows boot foundation"
                   Foreground="#9CA3AF"
                   HorizontalAlignment="Right"/>
    </Grid>
</Window>
